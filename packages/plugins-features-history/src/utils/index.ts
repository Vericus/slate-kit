import handleRedo from "./handleRedo";
import handleUndo from "./handleUndo";
import hasRedo from "./hasRedo";
import hasUndo from "./hasUndo";
import isRedo from "./isRedo";
import isUndo from "./isUndo";

export default {
  handleUndo,
  handleRedo,
  hasUndo,
  hasRedo,
  isRedo,
  isUndo
};

export { handleUndo, handleRedo, hasUndo, hasRedo, isRedo, isUndo };
